DISCORD_WEBHOOK_URL=
DISCORD_BOT_TOKEN=
DISCORD_CHANNEL_ID=

# Optional. Used by orchestrator when a TEST_RESULT failure arrives.
# The command receives HARNESS_EVENT_JSON in its environment.
DEV_AGENT_COMMAND=

# Optional. Used by orchestrator when a DEV_DONE event arrives.
# Defaults to: npm run harness:verify:all
TEST_AGENT_COMMAND=
